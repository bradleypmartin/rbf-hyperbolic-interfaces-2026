# paper/ — the manuscript (epic #51)

The arXiv-ready write-up of Part 3, the seed stencils of
[`docs/stiff-features.md`](../docs/stiff-features.md) (issue #27). Scaffolded
in #52; the sections are drafted one sub-issue at a time (table below).
Follows the `paper/` pattern of the author's
[weil-positivity-lab](https://github.com/bradleypmartin/weil-positivity-lab),
[bolza-bending](https://github.com/bradleypmartin/bolza-bending) and
[dirichlet-bridge](https://github.com/bradleypmartin/dirichlet-bridge).

**Novelty is cited, not claimed.** The literature pass (#53, 2026-09-20)
bucketed the claim in the root [`LITERATURE.md`](../LITERATURE.md): every
ingredient is prior work (§1a there), the combination is "no prior instance
found" (§1b), and §6 there holds the only novelty wording the manuscript
may use.

**Licensing.** `paper/` (`main.tex`, `references.bib`, `main.pdf`,
`figures/`) is [CC BY 4.0](LICENSE); the code in the rest of the repository
is MIT (see the root [README](../README.md)).

## Source-of-truth rule

`docs/stiff-features.md` stays canonical. The manuscript **quotes** it and
never becomes a second source of truth. Every number in `main.tex` traces to
a notes section or to the results cache of #54 (a `% TRACE` comment names
the source). The assembly pass (#61) re-checked all of them, and the ones
the cache holds are asserted by `scripts/paper_numbers.py` (below).

## Files

| File | What it is |
|---|---|
| `main.tex` | The manuscript (single source file; amsart). No stubs remain since #62. |
| `references.bib` | Bibliography, 66 entries, 59 of them cited (the scaffold's `\nocite{*}` went in #61; the seven entries no `\cite` reaches stay in the file for `LITERATURE.md`). Every entry carries a dated `VERIFIED` comment naming the primary source fetched for it in #53 (Crossref record, arXiv record, archive.org / zbMATH record, or the PDF); fields no fetch confirmed are left out and the comment says so. No unverified citation ships; `LITERATURE.md` §2 P5 is the log. |
| `main.pdf` | The built PDF (committed deliverable). |
| `make_arxiv.py` | arXiv packaging (#62): stages `main.tex`, `main.bbl`, `references.bib`, the figures and the `tab_*.tex` fragments into `arxiv/` with whole-line comments stripped, runs the repository checks, rebuilds the staged tree and compares it with `main.pdf`, then writes `arxiv.tar.gz`. |
| `data/` | The results cache (#54): one JSON per driver run (errors, rates, truncation and snapshot numbers, spectra summaries, with the run's arguments, date and git SHA), written by the stiff drivers with `--data-dir paper/data`. Schema in `src/pdes_demo/results_cache.py`. |
| `figures/` | Generated by `scripts/paper_figures.py` from `data/` and the drivers, never hand-edited: vector figures (`*.pdf`) and booktabs table fragments (`tab_*.tex`, for `\input`). See `figures/README.md`. |
| `LICENSE` | CC BY 4.0 for this directory. |

## Build (tectonic, primary)

```bash
cd paper/
tectonic main.tex                       # quick build
tectonic --keep-intermediates main.tex  # keeps main.bbl (needed for packaging)
```

Fallback (TeX Live / MiKTeX): `latexmk -pdf main.tex`.

**Pitfall (#52, #61).** tectonic 0.16.9's BibTeX hangs forever, at full CPU
with no log, on an `amsalpha` label whose three-letter prefix contains a
braced accent in a single author's surname (`P{\'o}lya`), and the unbraced
form (`P\'olya`) avoids the hang but prints the label as `[P\'22]`. The two
single-author entries concerned carry the accented letter as UTF-8
(`Pólya`, `Mühlbach`), which labels as `[Pó22]` and `[Mü73]`. If a build
spins for more than a minute, bisect `references.bib`.

Intermediates (`.aux`, `.bbl`, `.log`, …) are gitignored; `main.pdf` is
committed on every change to `main.tex` or `references.bib`. The `\date` is
fixed by hand, never `\today`, so the committed PDF is reproducible from the
committed source.

## Figures and tables (#54)

Everything under `figures/` regenerates from committed sources:

```bash
uv run python scripts/paper_figures.py            # from data/: ~10 s
uv run python scripts/paper_figures.py --all      # + stills and spectra through the
                                                  # drivers (needs outputs/ caches): ~50 min,
                                                  # the two n = 2500 spectra most of it
uv run python scripts/paper_figures.py --check    # regenerate the cached set elsewhere,
                                                  # fail on any byte that differs
```

The convergence figures, the two seed-basis figures and every `tab_*.tex`
come from `data/` alone. The stills and the spectra need simulation state,
so `--all` runs the drivers in print style (`--style print --format pdf`)
against the reference and operator caches under `outputs/` and copies their
PDFs in. Print figures pin `SOURCE_DATE_EPOCH=0`, so a regenerated PDF is
byte-identical to the committed one; `--check` is the gate #61 scripts.

`data/` itself is refreshed by the drivers, from the caches under
`outputs/` (see CLAUDE.md for the runtimes):

```bash
uv run python scripts/wave1d_stiff.py --data-dir paper/data
uv run python scripts/wave2d_stiff.py --data-dir paper/data
uv run python scripts/wave2d_stiff.py --direction 1 2 --widths 0.0025 0.01 \
    --modes naive aware ablate --seed-floor --data-dir paper/data
uv run python scripts/wave2d_stiff.py --amplitude 0.02 --widths 0 0.0025 0.005 0.01 \
    --seed-floor --truncation --snapshot-width 0.005 --data-dir paper/data
uv run python scripts/wave2d_stiff.py --amplitude 0.02 --widths 0.005 --seed-rtol 1e-3 \
    --no-snapshot --data-dir paper/data
uv run python scripts/wave2d_stiff_eigenvalues.py --n 2500 --run --floor --data-dir paper/data
uv run python scripts/wave2d_stiff_eigenvalues.py --n 2500 --run --amplitude 0.02 \
    --data-dir paper/data
uv run python scripts/wave2d_stiff_eigenvalues.py --n 900 --widths 0.125 0.25 0.5 1 2 \
    --variants seeds hyper19 naive-hyper seeds30 naive19 --data-dir paper/data
```

Numbers in `main.tex` quote the notes and, where the notes round, the
cache's unrounded errors (each `% TRACE` comment says which). The
scripted half of the assembly check is

```bash
uv run python scripts/paper_numbers.py        # every cache-backed number the text quotes
```

which recomputes each ratio, rate and factor the manuscript states from
`data/` and fails on any that no longer rounds to the quoted value; add a
line to its table whenever the text quotes a new one. The notes' floor and
seed-floor rows of §5.4–5.6 carried hand-derived rates that differed from
the cache's by up to 0.2; #61 corrected the notes from the cache, so the
two agree.

## Work breakdown (epic #51)

| Sub-issue | Scope |
|---|---|
| #52 | Scaffold: this directory, the skeleton, the seeded bibliography, licence, packaging |
| #53 | Literature and novelty pass: `LITERATURE.md`, bibliography verified |
| #54 | Figures, tables and a results cache: vector figures from committed scripts |
| #55 | §1–2: introduction and setting |
| #56 | §3: the seeds in one dimension |
| #57 | §4: one-dimensional results (the knee) |
| #58 | §5: the seeds in two dimensions |
| #59 | §6: two-dimensional results |
| #60 | §7: discussion, limitations, future work, conclusions |
| #61 | Assembly and consistency pass |
| #62 | arXiv packaging and the pre-submission decisions |

## arXiv packaging (#62)

```bash
cd paper/
tectonic --keep-intermediates main.tex   # produces main.bbl
uv run python make_arxiv.py              # ~1 min: repository checks, staging, rebuild gate
```

arXiv builds from the uploaded tree and does not run BibTeX, so the upload
carries the built `main.bbl`. The script stages `main.tex`, `main.bbl`,
`references.bib`, the 13 figures and the 18 `figures/tab_*.tex` fragments
(same layout, so `\graphicspath` and `\input{figures/...}` resolve) into a
clean `arxiv/` and writes `arxiv.tar.gz` (2.3 MB). Both are gitignored.

What it does beyond copying, each a gate rather than a hope:

- **Comments are stripped** (322 whole-line comments from `main.tex`, 267
  from `references.bib`, the `GENERATED` header of each fragment). The
  `% TRACE` and `% NOTATION` blocks and the dated `VERIFIED` notes are
  load-bearing *in the repository*, where the ledger and this README point
  at them; they are not part of the paper, and arXiv republishes source
  verbatim. Inline comments stay (removing a line's tail can change a
  newline). `--keep-comments` disables.
- **The repository checks run first**: `scripts/paper_numbers.py --quiet`
  (every cache-backed number the text quotes) and
  `scripts/paper_figures.py --check` (byte identity of the figures and
  fragments with what the cache regenerates).
- **Draft markers are refused** (`\stub`, `\todo`, `[Stub,`).
- **The staged tree is rebuilt** in a scratch copy with tectonic and its
  `pdftotext` output compared with the committed `main.pdf`; any difference
  fails, with the first lines of the diff. `references.bib` ships because
  this rebuild re-runs BibTeX and would otherwise produce an empty
  bibliography; it is inert on arXiv.
- **The accented labels are checked to render** in the rebuilt PDF
  (`[Mü73]`, `[Pó22]`, UTF-8 in the `.bbl` since #61). The rebuild is
  XeTeX; arXiv builds with pdfTeX, which reads UTF-8 natively since TeX
  Live 2018, and `main.tex` loads `inputenc` under `\ifPDFTeX` to say so.
  The script refuses any character outside what pdfTeX's `utf8` encoding
  maps (a Greek letter in text mode would fail there while XeTeX accepts
  it); none is present.
- **`\date` is printed**, so it gets set to the submission date on purpose.

`--no-verify` skips every gate; not recommended.

## Pre-submission decisions (#62, 2026-09-20)

Recorded here so the author block, the acknowledgments and the disclosure
section of `main.tex` can be read against them.

- **Authorship.** Bradley P. Martin is the sole human author. No AI tool
  is an author (arXiv's policy, point 3).
- **Acknowledgments.** Bengt Fornberg, for the 2016-era work the
  construction extends (the dissertation and the JCP 2017 interface
  treatment). Nothing else.
- **AI assistance** is disclosed in a section of its own, *Tool and
  computational resource disclosure*, after the acknowledgments, under the
  headings *Tools*, *Division of labour*, *Verification*, *Attribution*,
  *Responsibility*. Form and sources:
  - arXiv's [policy for authors' use of generative AI language
    tools](https://info.arxiv.org/help/moderation/index.html#policy-for-authors-use-of-generative-ai-language-tools)
    (checked 2026-09-20): report any significant use of text-to-text
    generative AI "consistent with subject standards for methodology";
    each author "takes full responsibility for all its contents,
    irrespective of how the contents were generated"; such tools "should
    not be listed as an author". Since May 2026 arXiv treats unverified
    LLM output (hallucinated references, leftover model chatter) as
    grounds for a one-year ban ([Research
    Information](https://www.researchinformation.info/news/arxiv-imposes-one-year-ban-for-unchecked-ai-generated-content/),
    2026-05-18, quoting the chair of the CS section).
  - The [Leiden Declaration on Artificial Intelligence and
    Mathematics](https://leidendeclaration.ai/) (2 June 2026, endorsed by
    the IMU) asks authors to "transparently disclose the use of automated
    tools" in a *Tool and computational resource disclosure* section, to
    put "proactive effort" into finding and crediting the sources the
    tools do not cite, and holds that responsibility "remains exclusively
    with the human authors". The section follows it item by item.
  - What recent submissions do (surveyed 2026-09-20 through the arXiv API
    and the papers' HTML; in math.AP in particular the practice has moved
    within the last month to detailed, model-specific statements):

    | Paper | Where | What it states |
    |---|---|---|
    | Giardi, Székelyhidi, *Onsager's conjecture for ideal MHD*, [2609.19506](https://arxiv.org/abs/2609.19506) (math.AP, 2026-09-16) | §1.4 "AI declaration" | Claude Fable 5.1 and ChatGPT Astra; the contributions itemised; "the mistakes are our own" |
    | Mugnai, *Confined elastic wires of large length*, [2609.10605](https://arxiv.org/abs/2609.10605) (math.AP, 2026-09-13) | §7 "Tool and computational resource disclosure", citing the Leiden Declaration | model IDs and dates of use; a division of labour; the attribution effort; every reference read by the author; responsibility |
    | An, Wang, Ye, Zhang, Zhuang, *Carleman estimates ... AI-assisted weights*, [2609.06283](https://arxiv.org/abs/2609.06283) (math.AP, 2026-09-05) | "Declaration of generative AI and AI-assisted technology" in the acknowledgments | the AI's role scoped to proposing candidate weights; all certification by the authors |
    | Tran, Yu, *L∞ variational approximation of the Aubry set*, [2609.01557](https://arxiv.org/abs/2609.01557) (math.AP, 2026-09-01) | acknowledgments | "the use of ChatGPT 5.6 Plus for suggesting some ideas and assisting with verifications ... All mathematical results and the final writing and revision of the manuscript are due to the authors" |
    | Dolean, Tabeart, *Advanced linear algebra with applications I*, [2608.21234](https://arxiv.org/abs/2608.21234) (math.NA, 2026-08-24) | preface, "A note on the use of AI tools" | Claude Opus 5 for references, language and graphics; "All statements, proofs and references have been checked by the authors, who take full responsibility" |
    | Ong, Xiao, *Agentic porting ... Outram Park*, [2608.17504](https://arxiv.org/abs/2608.17504) (physics.comp-ph, 2026-09-17) | methods sections and a V&V statement | Claude Code (Opus, Sonnet, Haiku) porting with a human in the loop; "retained full responsibility" |
    | Bui-Thanh, *The AI research assistant*, [2602.22842](https://arxiv.org/abs/2602.22842) (2026-04-15) | acknowledgments | "Claude (Anthropic) and ChatGPT (OpenAI) as AI research assistants ... All final collaborative mathematical results were verified by the author" |

    The arXiv *Comments* field is the other place a sentence goes: "the
    main results of this paper were obtained through a series of chats
    with ChatGPT" ([2609.15563](https://arxiv.org/abs/2609.15563)),
    "generated by ChatGPT 5.6 Sol on August 5, 2026"
    ([2608.24393](https://arxiv.org/abs/2608.24393)), "adversarial proof
    audit with Claude Fable 5"
    ([2608.23506](https://arxiv.org/abs/2608.23506)). The wider numbers
    are less encouraging: a PNAS study reported in March 2026 found a
    disclosure statement in about 0.1% of 75,172 journal papers since
    2023 while about 70% of journals have a policy
    ([CASRAI](https://casrai.org/news/ai-disclosure-gap-policy-vs-compliance)),
    and estimates of LLM-modified text in arXiv non-review papers rose
    from 6.2% (2023) to 18.9% (2025)
    ([2601.17036](https://arxiv.org/abs/2601.17036)). This manuscript
    sides with the detailed form.
- **Category.** Primary `math.NA` (arXiv aliases it to `cs.NA`, so it
  lists there too); cross-list `physics.comp-ph`. Not `physics.geo-ph`:
  the seismic connection is only the comparators of §4 and §6.
- **Endorsement.** Expected to be needed. arXiv's endorsement policy
  ([updated 2026-01-21](https://blog.arxiv.org/2026/01/21/attention-authors-updated-endorsement-policy/))
  auto-endorses only an institutional address paired with ownership of an
  earlier paper in the domain; the author has neither (no earlier arXiv
  paper under this name, a Gmail address). Path: start the submission,
  take the endorsement request it issues, and ask an established math.NA
  author (the RBF-FD literature of §2 is the natural pool; an abstract
  page answers "Which authors of this paper are endorsers?").
- **Licence at upload.** CC BY 4.0, matching [`LICENSE`](LICENSE). arXiv's
  choice is irrevocable.
- **Repository link.** §1's contribution 6 points at the tag
  `manuscript-v1` (GPT-6 Astra's #61 finding: a versioned link, not the
  moving default branch). A Zenodo DOI is a v2 option.
- **Repo home.** The manuscript stays in this repository for v1; the
  code, tests and notes it quotes are here.
- **`\date`.** Hand-fixed; `make_arxiv.py` prints it. Set it to the
  submission date, rebuild, commit `main.tex` and `main.pdf` together,
  then tag.
- **Length.** 64 pages. arXiv has no page limit, so the three σ_yy tables
  (`tab:flat2d_syy`, `tab:oblique2d_syy`, `tab:curved2d_syy`) stay.
- **Figure size.** The two n = 2500 spectra PDFs are 880 KB each
  (12,500-point scatters); the tarball is 2.3 MB, well inside arXiv's
  limits, so they stay vector.
- **Comments and MSC (optional metadata).** Comments: "64 pages, 13
  figures. Code, tests and results cache at the repository link in the
  paper. Written with AI assistance, disclosed in the paper." MSC 2020
  candidates, to confirm against the list at upload: 65M06, 65D25, 35L05,
  74J05.

## Submitting

1. Merge the #62 pull request. On the submission day, if it is no longer
   2026-09-20, set `\date`, rebuild, commit `main.tex` and `main.pdf`.
2. Tag that commit on `main` and push the tag:
   `git tag -a manuscript-v1 -m "manuscript v1, as submitted to arXiv" && git push origin manuscript-v1`.
3. From `paper/` at that commit:
   `tectonic --keep-intermediates main.tex && uv run python make_arxiv.py`;
   every gate green.
4. Upload `arxiv.tar.gz`; primary `math.NA`, cross-list `physics.comp-ph`;
   licence CC BY 4.0; the abstract from `main.tex` (plain text with `$...$`
   math); comments as above.
5. Check arXiv's processed PDF before announcing: it is the pdfTeX build
   the local gate cannot run. Look at the labels `[Mü73]` and `[Pó22]`, one
   table per section and the two spectra figures.
6. When the identifier arrives, add it to the root README and CLAUDE.md.
